import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    background: {
        backgroundColor: "white",
        flexGrow: 1,
    },
    input: {
        paddingBottom: 100,
        marginBottom: 10,
        backgroundColor: "white",
        fontSize: 18,
    },
});
