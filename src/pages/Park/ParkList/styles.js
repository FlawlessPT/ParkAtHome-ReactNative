import { StyleSheet } from "react-native"

export const styles = StyleSheet.create({
    background: {
        backgroundColor: "white",
        flexGrow: 1,
    },
    parkImage: {
        height: 90,
        width: 90,
        borderRadius: 20,
    },
})